description: 200 OK
content:
  application/json:
    schema:
      type: object
      properties:
        users:
          description: ユーザー一覧
          type: array
          items:
            $ref: '../../schemas/user.yaml'
